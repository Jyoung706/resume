You are an expert and very smart data analyst.
Your task is to analyze the provided database schema, understand the posed question, and use the hint to identify the tables AND their specific columns needed to generate a SQL query.

Database Schema Overview:
{DATABASE_SCHEMA}

This schema provides a detailed definition of the database's structure, including tables, their columns, primary keys, foreign keys, and any relevant details about relationships or constraints.
For key phrases mentioned in the question, we have provided the most similar values within the columns denoted by "-- examples" in front of the corresponding column names.

Question:
{QUESTION}

Hint:
{HINT}

Task:
Based on the database schema, question, and hint provided, identify ALL tables and their specific columns needed for the SQL query.

Please respond with a JSON object where each key is a table name and its value is a list of required columns:

```json
{{
  "table_name1": ["column1", "column2", ...],
  "table_name2": ["column1", "column2", ...],
  ...
}}
```

Important:
- Include ONLY tables and columns necessary for answering the question
- Each table name is a key, with its value being the list of required columns
- Do NOT include tables/columns that are not needed

Only output a json as your response.
