Summarize the following question and answer separately in {LANGUAGE}.
Each summary must be within {LIMIT_CHARACTERS} characters.

Guidelines:
- Create concise summaries for both the question and answer
- Focus on core meaning and intent, not format
- For SQL query answers: describe what the query computes, aggregates, or filters in natural language. Explain the data operations (JOIN, GROUP BY, WHERE conditions, etc.) rather than the SQL syntax itself
- For general answers: summarize the key information or conclusion provided
- Do not use emojis or unnecessary subject expressions
- Output as JSON with keys "q" (question summary) and "a" (answer summary)

Few-shot examples:

Example 1 (SQL - Aggregation with filtering):
QUESTION: "2023년에 가장 많은 매출을 올린 상위 5개 제품은?"
ANSWER: "SELECT product_name, SUM(sales) as total_sales FROM sales WHERE year = 2023 GROUP BY product_id ORDER BY total_sales DESC LIMIT 5"
Output:
{{"q": "2023년 매출 상위 5개 제품 조회", "a": "2023년으로 필터링하여 제품별 매출 합계를 계산하고 내림차순 정렬 후 상위 5개 추출"}}

Example 2 (General):
QUESTION: "데이터베이스 인덱스는 왜 필요한가요?"
ANSWER: "데이터베이스 인덱스는 쿼리 성능을 향상시키기 위해 필요합니다. 인덱스를 사용하면 전체 테이블을 스캔하지 않고 필요한 데이터를 빠르게 찾을 수 있습니다."
Output:
{{"q": "데이터베이스 인덱스의 필요성", "a": "전체 테이블 스캔 없이 빠른 데이터 검색을 통한 쿼리 성능 향상"}}

Example 3 (SQL - JOIN with aggregation):
QUESTION: "각 부서별 평균 급여는 얼마인가요?"
ANSWER: "SELECT d.department_name, AVG(e.salary) as avg_salary FROM employees e JOIN departments d ON e.dept_id = d.id GROUP BY d.id"
Output:
{{"q": "부서별 평균 급여 조회", "a": "직원과 부서 테이블을 조인하여 부서별로 그룹화하고 급여 평균값 계산"}}

Example 4 (SQL - Time-based filtering with counting):
QUESTION: "지난달 서울 지역 주문 건수는?"
ANSWER: "SELECT COUNT(*) as order_count FROM orders WHERE region = 'Seoul' AND order_date >= DATE_SUB(CURDATE(), INTERVAL 1 MONTH)"
Output:
{{"q": "지난달 서울 지역 주문 건수 조회", "a": "서울 지역으로 필터링하고 지난 한 달 기간 조건으로 주문 건수 집계"}}

Input:
QUESTION: {QUESTION}
ANSWER: {ANSWER}

Output only valid JSON. Do not include any other text or explanation.