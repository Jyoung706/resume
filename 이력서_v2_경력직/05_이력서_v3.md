# 전준영 | Backend / Infra Engineer

> 자체 백엔드 프레임워크를 설계하고 KT Cloud Kubernetes 플랫폼을 GitOps로 구축한 **인프라에 강한 백엔드 엔지니어** · 경력 약 3년

## Contact.

- **E-mail** | wnsdud9476@gmail.com
- **Phone** | 010-6564-5361
- **Github** | github.com/Jyoung706
- **경력기술서** | _(별도 문서)_

---

## About.

**프레임워크를 쓰는 것을 넘어 직접 설계하는 백엔드 엔지니어입니다.** Node.js/TypeScript 위에 Spring 스타일의 DI·선언적 트랜잭션·멀티 DB 추상화를 갖춘 자체 프레임워크 `@orkis/core`를 설계하며 동시성·트랜잭션 전파·DB 드라이버 내부 동작까지 파고들었고, 그 프레임워크 위에서 Text-to-SQL 플랫폼의 백엔드·인프라를 책임졌습니다.

KT Cloud ManagedKS 위에 클러스터·네트워킹·TLS·CI/CD·백업/DR까지 연구소 쿠버네티스 플랫폼을 GitOps로 코드화했고(98/123커밋, 약 80% 기여), 온프레미스에서 GitLab·Jenkins를 직접 구축한 경험을 클라우드 네이티브로 확장했습니다. **모르는 영역(새 언어·분산 시스템 디커플링)도 원인까지 파고들어 동작시키는 것**을 강점으로, AI 연구원·프론트엔드 개발자와는 이벤트 계약 기반으로 협업하며 서버를 양측의 접점으로 설계했습니다.

---

## Tech Skills.

- **Language** : TypeScript, JavaScript
- **Backend** : `@orkis/core`(자체 프레임워크), Express.js, WebSocket, SSE
- **Frontend** : React.js, Vue.js, Electron.js
- **Database** : PostgreSQL, MySQL/MariaDB, SQLite, Redis, MongoDB
- **Infra / DevOps** : **Kubernetes(KT Cloud ManagedKS)**, Docker, Podman, Helm, ingress-nginx, cert-manager, NFS Provisioner, GitLab, Jenkins, Nginx, PM2, Linux(Rocky·CentOS·Ubuntu)
- **CI/CD / Ops** : GitOps(IaC), 크로스 네임스페이스 RBAC, CronJob 백업/DR, metrics-server·OpenLens 모니터링
- **AI/LLM 연동** : RAG 서버 연동, LLM 챗 SSE 스트리밍 연동, Anthropic·OpenAI SDK
- **Collaboration** : Slack, Notion, Figma, Postman

---

## Work Experience.

### (주)범익 — 기업부설 연구소 / 연구원

**2025.02 ~ 재직 중 (약 1년 4개월)** · Server & Infra Engineer

자연어를 SQL로 변환·실행하는 Text-to-SQL 연구 플랫폼 **"Orkis"** 개발. 자체 백엔드 프레임워크 설계부터 데스크탑 앱, k8s 배포까지 백엔드·인프라 전반을 담당. _(SQL 생성·RAG 모델링 및 클라우드 채팅 파이프라인은 AI/타 개발자 담당, 본인은 백엔드 인프라·시스템 연결·배포를 담당)_

**자체 백엔드 프레임워크 `@orkis/core` 설계·구현** · 305커밋, 데코레이터 30종

- 성숙한 ORM/프레임워크(Nest·TypeORM)가 **요청별 동적 커넥션(멀티테넌시)·멀티 DB 추상화** 요구를 충족하지 못해, 핵심만 자체 구현하기로 결정
- Spring 스타일 **DI 컨테이너** — `@Controller`/`@Service`/`@Dao`/`@Autowired` + ComponentScan 자동 빈 등록, 순환참조 감지, 싱글톤/프로토타입 스코프
- **선언적 트랜잭션 `@Transactional`** — REQUIRED/REQUIRES_NEW/SUPPORTS 전파 정책을, 실제 접근 시점에만 Bean을 clone하는 Lazy Proxy로 격리·전파
- **멀티 DB 추상화** — PostgreSQL·SQLite·MariaDB·Redis·File 5종을 단일 `BaseAdapter`로 통일, placeholder 정규화·트랜잭션 지원 분기
- **동적 DB 연결(멀티테넌시)** — 요청 userId로 런타임에 사용자별 connection을 생성하는 Text-to-SQL 핵심 인프라
- **검증** — 멀티 DB·스케줄러·동적 커넥션 통합 테스트/예제 30종 구축, 트랜잭션 전파 5개 시나리오를 PostgreSQL `xmin` 기반으로 격리 검증

**백엔드 애플리케이션(orkis-backend) — 인증·동적DB·빌드·배포 담당**

- 네이버·카카오·구글 **OAuth 소셜 로그인**을 단일 오케스트레이터(`AuthService`)로 통합, 제공사별 응답 정규화 + bcrypt·Bearer 병행
- **동적 DB 연결(멀티테넌시)** — 사용자별 DB를 런타임 등록, 접속정보 **AES-256-GCM 암호화**, PostgreSQL·MySQL/MariaDB·SQLite 타입별 어댑터 분기
- **단일 코드베이스로 Cloud(PG+Redis)·Desktop(SQLite+File) 동시 지원** — TS `paths` fallback + `tsc-alias`로 같은 import를 빌드 타깃별로 자동 해석. "exclude가 transitive import를 막지 못한다"는 컴파일러 동작을 진단해 폴더 단위 cascade exclude로 해결
- 위 구조 위에서 **데스크탑 채팅을 Redis 없이 AI SSE를 직접 구독하도록 어댑테이션**해 프론트 코드 무수정으로 Cloud와 동일 이벤트 계약 유지
- 멀티스테이지 Docker(Cloud/Desktop 분리 빌드)·k8s 무중단 롤링(maxSurge:1/maxUnavailable:0) 배포

**백그라운드 작업 서비스(orkis-jobs) 단독 개발** · 68커밋

- 회복탄력 Redis 인프라(backoff+jitter 재시도·헬스체크), 세션 정리/아카이빙 Job 5종
- SCAN(논블로킹)+Pipeline 배칭으로 세션 정리를 이벤트 루프 블로킹 없이 처리, 실패 시 순차 폴백
- 수동 객체 관리 → 데코레이터 기반 DI 아키텍처로 마이그레이션하며 **레거시 1,000줄+ 제거**

**Electron 데스크탑 앱(orkis-desktop) 단독 설계·구현** · 91커밋

- Electron 39 + Podman 컨테이너 런처, mac/Windows(NAT·mirrored) 크로스플랫폼 네트워킹을 단일 함수로 추상화
- `PodmanEventsMonitor`로 `podman events` 스트림을 파싱해 서비스 헬스를 IPC로 push(이벤트 기반 ready 판정)
- **통신 인프라 마이그레이션** — 자체 TCP 소켓(wire format·shared secret) 전면 제거 → 표준 HTTP REST/SSE 전환(데스크탑 AI 백엔드와 양 끝단 동시), 유지보수성·표준화 확보

**클라우드 AI 백엔드를 데스크탑 단일 머신용으로 분리·이식(orkis-desktop-ai)** · 54커밋

- 기존 클라우드 AI(orkis-ai)가 **Ray(분산 액터)·Redis에 강결합**돼 있던 것을, 데스크탑(단일 머신, Ray/Redis 없음)에서 동작하도록 **디커플링·이식**
  - Ray Actor(6종 워커) → **asyncio Task + TaskManager 추상화**
  - Redis 이벤트 스트림(XADD) → **asyncio.Queue + SSE 스트리밍**
  - PostgreSQL/Redis → **SQLite/파일**
- 비즈니스 로직(LangGraph 워크플로)은 원본을 재사용하고, `EventTransport` ABC로 전송 계층을 추상화해 프로토콜 교체가 가능하도록 설계
- **새 언어(Python·asyncio)를 학습해 이식·통합** — 소스코드 기준 상주 메모리 **약 4.5GB → 2GB(약 60%) 절감 추정**, 다중 프로세스(Ray head+워커+Redis) → 단일 uvicorn 프로세스로 배포·디버깅 단순화 _(추정: Ray/Redis 런타임 제거 + `ray_config` 예약 메모리 기준, 실측 아님)_

**연구소 Kubernetes 플랫폼 GitOps 구축 (IaC)** · 98/123커밋(약 80% 기여, AI/전처리 매니페스트는 동료와 분담)

- KT Cloud ManagedKS 클러스터(7노드)를 매니페스트로 코드화 — **노드풀 5종**(ingress/jenkins/gitlab/database/ai)을 `nodeSelector`로 분리해 진입점·CI·DB·앱 자원 간섭 차단
- ingress-nginx **DaemonSet + hostNetwork**(L4 LB 패스스루) + cert-manager/Let's Encrypt **TLS 자동 발급·갱신**, SSE 전용 인그레스(Sticky Session·proxy-buffering off·timeout 1800s) 튜닝
- NFS 동적 프로비저닝 **StorageClass 4종**(운영 Retain/개발 Delete/정적/로컬), PostgreSQL 17 + Redis 3종(chat/stage/session) 운영
- **GitLab·Jenkins 자체 호스팅** — Jenkins Kubernetes Plugin으로 빌드마다 Agent Pod를 동적 생성→소멸(빌드 동시성 확보), 크로스 네임스페이스 최소 권한 RBAC로 배포 권한 격리
- **백업/DR 3종 자동화** — PG 주간 덤프(14일 보존), GitLab 분기 전체 백업, **토큰을 디스크에 남기지 않는 GitLab 미러 일간 백업**(임의 git 호스트로 `push --mirror` 복원 가능한 DR 체계)

---

### (주)E3TS — Server Developer

**2023.06 ~ 2024.08 (1년 2개월)**

병원 전문 그룹웨어·콜센터 솔루션 개발. 온프레미스 인프라 구축과 그룹웨어 채팅·주차관제를 풀스택으로 담당.

**그룹웨어 채팅 — 서버·클라이언트 풀스택 단독** · 서버 309커밋 / 클라이언트 406커밋

- **PM2 클러스터 + Redis 어댑터 부하분산** — `@socket.io/redis-adapter`로 다중 프로세스 간 메시지 브로드캐스트, CPU 코어 절반만큼 인스턴스 생성·자동 재시작(메모리 1GB 임계), **분산 락**으로 클러스터 내 cron 중복 실행 방지
- **회사별 멀티테넌시** — Socket.io **namespace**로 회사(`company_id`)별 독립 통신 채널 격리, Redis로 네임스페이스별 사용자 온/오프라인 상태 추적
- **인증** — JWT 만료 시 catch-and-refresh로 소켓 연결 끊김 없이 자동 갱신, MongoDB(메시지)+MySQL(사용자/회사) 하이브리드
- **클라이언트** — Vue 3 + Pinia `socketStore`로 다중 namespace 동시 연결 관리(9종 수신 이벤트), Electron 31 + electron-builder로 mac(dmg)/Windows(NSIS) 크로스플랫폼 빌드(IPC·파일명 충돌 처리·시스템 트레이)

**병원 발렛파킹 주차관제 시스템** · 385커밋

- **Layered 3계층 아키텍처**(Controller 21 / Service 21 / DAO 22)로 **REST API 119개** 구현, HTTP·WebSocket을 동일 비즈니스 로직으로 통합
- Socket.io 실시간 주차 현황 반영 + JWT 인증·중복 로그인 감지·강제 종료, TypeORM 다중 데이터소스(main + SMS), Web Push 알림

**사내 인프라/형상관리 도입 (주도)**

- 사내 Git 도입을 직접 건의·주도, **GitLab CE·Jenkins 온프레미스 단독 구축** — ssh-remote 공유 작업을 무중단 배포 환경으로 전환
- GitLab Webhook + Jenkins CI/CD 파이프라인, Nginx 웹서버·도메인 SSL·DNS 구성, 팀원에게 Git flow 교육
- 그룹웨어 메일(Postfix·Dovecot·Roundcube + Emailengine 릴레이) 구축, SPF/DKIM/DMARC·WAF(ModSecurity) 대응

---

## 대표 성과.

### 1. Node.js에 Spring `@Transactional`을 자체 구현 — 트랜잭션 전파 + Lazy Proxy

- **문제** 멀티테넌시(요청별 동적 커넥션)와 멀티 DB 추상화 요구를 기존 ORM/프레임워크가 충족하지 못함
- **행동** JavaScript에 없는 트랜잭션 컨텍스트 전파를 Proxy/Reflect 메타프로그래밍으로 구현. REQUIRED/REQUIRES_NEW/SUPPORTS 전파 정책 지원, 실제 접근 시점에만 clone하는 Lazy 전략으로 격리
- **결과** 리팩토링이 부른 격리 회귀를 PostgreSQL `xmin` 기반으로 두 서비스의 독립 트랜잭션을 입증해 해결. 동일 패턴을 5개 시나리오 테스트로 회귀 방지 `@orkis/core`

### 2. 클라우드 강결합 AI 백엔드를 데스크탑 단일 머신으로 디커플링

- **문제** Ray(분산 액터)·Redis에 강결합된 클라우드 RAG 서버를, Ray/Redis가 없는 데스크탑 단일 머신에서 동작시켜야 함
- **행동** Ray Actor→asyncio Task, Redis 스트림→asyncio.Queue+SSE, PG/Redis→SQLite/파일로 대체. 비즈니스 로직은 재사용하고 `EventTransport` ABC로 전송 계층을 추상화. 새 언어(Python)를 학습해 이식
- **결과** Ray/Redis 의존을 제거해 **단일 uvicorn 프로세스로 통합** — 배포·디버깅 단순화, 비즈니스 로직 100% 재사용. 상주 메모리도 소스 기준 약 60% 절감 추정(~4.5GB→2GB) `orkis-desktop-ai`

### 3. 연구소 Kubernetes 플랫폼 GitOps 구축

- **문제** 연구 플랫폼의 인프라(클러스터·CI/CD·데이터·백업)를 재현 가능하게 코드로 관리해야 함
- **행동** KT Cloud ManagedKS 위에 노드풀 분리·ingress·TLS 자동화·GitLab/Jenkins 자체 호스팅·백업 전체를 매니페스트로 코드화
- **결과** 환경을 코드로 재현 가능한 GitOps 체계 확보, 토큰 비노출 미러 백업으로 임의 git 호스트 복원 가능한 DR 구축 `k8s-config`(약 80% 기여)

---

## Education.

- **호서대학교 세무회계학과 졸업 (4년)** — 학점 3.86 / 4.5
- **위코드(wecode) 백엔드 개발자 과정 6기** *(2022.07 ~ 10)*
- 비전공에서 독학으로 백엔드 개발을 시작 *(2022.01 ~ 06)*, 모르는 영역도 원인까지 파고들어 동작시키는 것을 강점으로 삼습니다.
